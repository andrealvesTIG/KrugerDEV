Act as a senior solution architect and database analyst.

I want you to inspect this project and identify the full data schema used by the application.

Your task:

1. Scan the codebase and detect all schema definitions, including:
   - database schema files
   - ORM models
   - migration files
   - entity definitions
   - API request/response schemas
   - validation schemas
   - TypeScript interfaces/types related to persisted data
   - JSON schema or OpenAPI schema files

2. Determine which technologies are being used for schema management, such as:
   - Prisma
   - TypeORM
   - Sequelize
   - Mongoose
   - Drizzle
   - Entity Framework
   - SQLAlchemy
   - raw SQL migrations
   - Zod / Yup / Joi / Pydantic
   - OpenAPI / Swagger

3. Produce a structured report with:
   - detected schema source files
   - main entities/tables/collections
   - fields for each entity
   - data types
   - primary keys
   - foreign keys / relationships
   - indexes and unique constraints, if found
   - required vs optional fields
   - enums and default values
   - derived/computed fields if present

4. Cross-check for inconsistencies between:
   - database schema
   - backend models
   - validation schemas
   - API contracts
   - frontend types

5. Flag any issues such as:
   - duplicated schema definitions
   - mismatched field names or types
   - missing relations
   - nullable fields that should not be nullable
   - fields used in code but missing from schema
   - outdated migrations
   - dead tables/models no longer used
   - validation rules not matching persisted data
   - API schema drift vs database schema

6. If possible, infer:
   - the canonical source of truth for the schema
   - whether the project has schema drift
   - whether there are risky areas that could break production

7. Output the result in these sections:
   - Schema technologies detected
   - Canonical schema source
   - Entity/table overview
   - Relationship map
   - Inconsistencies found
   - Risks
   - Recommended fixes

8. Also provide:
   - a concise ERD-style text summary
   - top 10 most important schema files
   - a confidence level for each conclusion
   - anything you could not verify directly

Rules:
- Scan the full repository.
- Exclude folders unless they contain custom app schema logic:
  - node_modules
  - .git
  - dist
  - build
  - coverage
  - .next
  - out
- Do not guess if evidence is weak. Mark uncertain findings clearly.
- Prefer actual schema files and migrations over inferred usage in code.
- If multiple schema systems exist, explain which one appears authoritative.
- Use tables where useful.
- End with a practical action plan.

Please start by locating all schema-related files, then analyze them and produce the full report.