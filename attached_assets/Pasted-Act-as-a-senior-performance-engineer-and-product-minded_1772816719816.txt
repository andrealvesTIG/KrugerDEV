Act as a senior performance engineer and product-minded debugger. Analyze the specific page called "Project Details" in this codebase with a deep focus on performance, responsiveness, and user-perceived speed.

Your objective:
Thoroughly inspect the Project Details page and identify anything that could make it slow, heavy, laggy, inefficient, or unstable. Focus on root causes, not surface-level guesses.

Scope:
- Find the Project Details page entry point, route, component tree, data flow, and all dependencies.
- Analyze frontend rendering performance, backend/API performance, database/query performance, and network usage related to this page.
- Inspect anything loaded directly or indirectly by this page, including child components, hooks, state stores, API calls, images, charts, tables, modals, permissions logic, and background fetches.

What to investigate:
1. Frontend performance
- unnecessary re-renders
- large component trees
- expensive computations during render
- missing memoization where it matters
- oversized state or prop drilling
- blocking effects
- slow tables, lists, or cards
- poor virtualization for large datasets
- heavy charts or maps
- unnecessary DOM nodes
- large bundles or lazy-loading opportunities
- image/file preview inefficiencies
- bad loading-state behavior causing layout shift or jank

2. Data fetching and API performance
- duplicate API calls
- waterfall requests
- over-fetching data
- missing caching
- slow endpoints used by Project Details
- sequential requests that could be parallelized
- excessive polling or refetching
- poor error/retry handling affecting performance

3. Backend and database performance
- expensive queries
- N+1 query patterns
- missing indexes
- loading too much related data
- repeated permission checks
- slow aggregations or joins
- unnecessary serialization/deserialization work

4. User-perceived performance
- slow time to first useful content
- slow page transitions into Project Details
- blocking spinners
- lag when editing or switching tabs/sections
- sluggish interactions
- delayed saves or refreshes

How to work:
1. Find and inspect the Project Details page implementation.
2. Map how the page loads and what data it requests.
3. Identify the biggest performance bottlenecks ranked by impact.
4. For each bottleneck, provide:
   - Issue
   - Root cause
   - Evidence from code
   - Expected user impact
   - Recommended fix
   - Effort level
   - Risk level
5. Prefer concrete evidence from codepaths, queries, hooks, and component structure.
6. Make note of assumptions if runtime profiling tools are unavailable.
7. Suggest the highest-value quick wins first, then larger structural improvements.

Important rules:
- Do not give generic advice unless it clearly applies to this page.
- Be specific about file names, components, hooks, endpoints, queries, and patterns.
- Focus heavily on performance, but also call out reliability issues that may worsen performance.
- Do not refactor immediately. First perform a diagnosis.
- If you suggest code changes, keep them targeted and explain why they are likely to help.
- If exact runtime metrics are unavailable, infer likely hotspots from the implementation and label them clearly as inferred.

Output format:
Start with:
1. Where the Project Details page is implemented
2. How it loads data
3. The top 5 performance risks

Then provide a ranked report for each issue using this format:

## Performance Issue #[number]
Title:
Area: Frontend / API / Backend / Database / Network / UX
Severity: High / Medium / Low
Confidence: High / Medium / Low

What is happening:
Root cause:
Evidence:
Why it hurts performance:
Recommended fix:
Expected impact:
Effort:
Risk:

Then end with:
- Quick wins to try first
- Larger improvements worth planning
- Any missing information that would help confirm the analysis