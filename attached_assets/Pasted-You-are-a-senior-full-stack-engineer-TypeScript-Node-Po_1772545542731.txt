You are a senior full-stack engineer (TypeScript/Node/Postgres/React) specializing in performance and date correctness.

I’m pasting a performance assessment report below. Your job is to produce a set of code changes (patches/diffs) that address the remaining issues and harden date correctness. Implement the fixes, update tests, and add a benchmark harness.

Primary goals (do these first):
1) Replace workingDaysBetween loop with an O(1) arithmetic implementation
   - Must match the current semantics exactly (exclusive/inclusive endpoints). If semantics are unclear, implement both modes:
     - exclusiveBetween(start, end)
     - inclusiveBetween(start, end)
   - Provide unit tests covering:
     - same day
     - Fri→Mon
     - Sat→Sun
     - ranges crossing month/year boundaries
     - negative/ reversed ranges
     - null handling
   - Apply this replacement in BOTH server and client copies:
     - server/lib/workingDays.ts
     - client/src/lib/workingDays.ts
   - Confirm performance with the existing benchmark: 10k tasks should be near the “6.5ms” target.

2) Fix overdue filter timezone edge case
   - Current server-side overdue compares endDate < today using DB server date. This can differ from client by ±1 day.
   - Make overdue evaluation deterministic:
     - Treat schedule dates as “date-only” (no timezone).
     - Option A (preferred): pass client “today” as YYYY-MM-DD via query param (e.g. today=2026-03-03) and compare endDate < today in SQL.
     - Option B: compute “today” in UTC and document it. But keep it consistent across server+client.
   - Add tests for boundary at midnight and for users in different timezones.
   - Ensure backward compatibility when today is not provided.

3) Resolve duration inconsistency (off-by-one)
   - There are two functions: calculateDuration() (working days to add) and workingDaysBetween() (days between), and they count differently.
   - Decide on a single consistent definition used across UI + API:
     - Define whether Gantt bars are inclusive of start/end and how durations display.
   - Update both functions (or rename) so the difference is explicit:
     - workingDaysBetweenExclusive()
     - workingDaysSpanInclusive()
     - addWorkingDays(startDate, nDays)
   - Add tests for edge cases that previously caused off-by-one.

Secondary goals:
4) Wire client date filters to server-side filtering (already implemented server-side)
   - Identify where the Tasks page currently filters tasks locally in useMemo.
   - Change it to pass the query parameters to /api/tasks/all:
     - startDateFrom, startDateTo, endDateFrom, endDateTo, overdue, sortBy, sortOrder, pageSize, offset
   - Keep a fallback client-side filter for offline/mock mode only.
   - Add an integration test (or MSW test) verifying query params are sent and results are rendered.

5) Gantt virtualization for large datasets
   - Ensure virtual scrolling is enabled for all views where task rows can exceed ~500.
   - Avoid parsing/formatting dates in row components; ensure parent precomputes date objects/strings using useMemo.
   - Add performance marks (performance.mark/measure) around render and log only in dev.

6) Multi-org batched query parameter limit mitigation
   - If inArray() can exceed parameter limits, implement chunking:
     - chunk orgIds/taskIds into batches (e.g., 1k per batch)
     - union results in memory (or use a temp table / VALUES list if supported)
   - Add guardrails: log warning when batching occurs.

Deliverables:
- Provide clear diffs/patches per file (like git diff format).
- Add/extend unit tests (jest/vitest) for date math and overdue behavior.
- Update or add scripts/benchmark-dates.ts to compare old vs new working-day implementations and print timings for 1k/10k/100k.
- Provide a short “How to validate” checklist and the exact commands to run tests + benchmarks.

Important constraints:
- Keep all internal schedule dates as date-only strings (YYYY-MM-DD) across API boundaries.
- Do NOT parse ISO strings repeatedly inside render loops; precompute maps with useMemo.
- Avoid breaking API clients: changes must be backward compatible.
- Prefer SQL ORDER BY for large sorts; keep client sorting only for small in-memory lists.

Here is the performance assessment report context (use it as requirements):
[PASTE THE REPORT TEXT HERE]