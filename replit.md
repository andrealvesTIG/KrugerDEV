# FridayReport.AI - Project Portfolio Management Application

## Overview
FridayReport.AI is an enterprise-grade project portfolio management application designed for comprehensive oversight of projects, portfolios, risks, key dates, and issues. It aims to enhance decision-making and operational efficiency through robust tracking, clean data presentation, and a professional user interface. The project's vision is to become a leading tool in project portfolio management.

## User Preferences
Preferred communication style: Simple, everyday language.

## System Architecture

### UI/UX Decisions
The application features an enterprise-grade UI inspired by Linear and Asana, utilizing a page-based architecture with reusable UI components. Styling is managed with Tailwind CSS and shadcn/ui. Data visualizations are powered by Recharts, and animations by Framer Motion. Industry-specific landing pages are responsive and SEO-optimized. The PMO Radar visualization uses HTML Canvas for dynamic, animated displays supporting both light and dark themes.

### Technical Implementations
**Frontend**: Developed with React 18 and TypeScript, employing Wouter for routing, TanStack React Query for state management, React Hook Form with Zod for form handling, and `@tanstack/react-virtual` for virtual scrolling.
**Backend**: Built with Express.js and TypeScript, utilizing PostgreSQL with Drizzle ORM. It provides a RESTful API with typed routes and Express sessions for session management.
**Shared Code**: Common schemas, route definitions, and models are shared between frontend and backend.
**Data Model**: Supports Portfolios, Projects, hierarchical Tasks, Resources, Risks, Issues, Portfolio Key Dates, Change Requests, Project Documents, and Organizations. It includes multi-tenancy with soft-delete and role-based access control. Task Milestones are integrated into the tasks table, while Portfolio Key Dates are managed in a dedicated table with various types and statuses.
**Security**: Features soft-delete, mandatory email verification, bot protection, versioned user consent tracking, encrypted OAuth tokens, `helmet` HTTP security headers, and `express-rate-limit` on all auth endpoints. Password policy requires 8+ characters with uppercase, lowercase, and digit. Investor Room password stored as environment variable (no hardcoded fallback). All API endpoints enforce authentication and authorization with role-based access checks on write operations.
**Notification Engine**: Supports various notification types with severity levels and deduplication.
**Help & Feedback**: Provides a system for user support tickets with text and screenshots.
**Friday Academy Training & Certification**: Offers interactive training modules with progress tracking and PDF certificate generation, including quiz mechanics.
**Integration Settings**: Stores organization-scoped integration credentials and connection statuses with encrypted OAuth tokens.
**API Design**: Follows a consistent API error convention and maintains a comprehensive OpenAPI 3.0 specification with extensive documentation for endpoints, schemas, and tags, including requiredness annotations and pagination.
**Performance Optimizations**: Includes extensive database indexing, N+1 query fixes, `React.memo` and `useCallback` for UI components, bulk assignment fetching, virtual scrolling, memoized date parsing, server-side date filtering, and optimized batch SQL updates.
**Code Organization**: Critical oversized files are modularized, duplicate industry landing pages consolidated, and Vite `manualChunks` configured for vendor code splitting. Unused code is systematically removed.
**Testing**: Utilizes Vitest for testing, focusing on date calculation logic, performance, and cross-validation.
**Resource Management**: Tracks resource skills, availability, and utilization through Capacity Planning, Workload Dashboard, and Availability Calendar views.
**PMO Radar**: A dynamic risk visualization page displaying risks and issues on a radar-style interface with interactive filters, time projection, timeline playback, and drill-down capabilities. It includes a simulation engine for modeling project risk lifecycles and provides a filterable log review UI with summary stats and PDF export.
**Shared Risk Editing**: A consistent `EditRiskDialog` component is used across the application for risk management, including AI suggestions, resource assignments, and change history.
**Issues Grid View**: The `/issues` page features an inline-editable data grid for managing issues and risks, with user-configurable column visibility and preference persistence.
**Issues Export/Import**: Supports exporting and importing filtered issues/risks to/from Excel (.xlsx) or CSV (.csv) with a preview dialog.
**CSV Import/Export Round-Trip**: Allows exporting tasks to CSV from project details and re-importing modified tasks.
**Task Date Scheduling Rules**: Implements rules for task date changes, dependency cascading, and milestone preservation while respecting working days.
**Gantt Undo/Redo**: A comprehensive undo/redo system for Gantt chart operations including field updates, reordering, indent/outdent, creation, deletion, and dependency management.
**Gantt Excel-like Grid Editing**: Cell-level focus (blue ring), arrow key/Tab navigation, Shift+Click/Arrow range selection (light blue highlight), type-to-edit and Enter-to-edit, cell-aware copy (Ctrl+C copies focused cell, range, or selected rows) and paste (Ctrl+V starts at focused cell's row and column, with progress dialog and cancel support). Read-only columns (WBS, #, Critical, Summary) are skipped during navigation. Estimated Hours is editable and clearable. Resources column is selectable/pasteable. Paste supports resource assignment (auto-match or create). Backward compatible with row-level checkbox copy/paste.
**Manual Task Scheduling**: Tasks support `schedulingMode` ('auto'|'manual'). Manually scheduled tasks can exist without start/end dates. Toggle via task action menu in Gantt view. Timeline shows "Manually Scheduled" badge for dateless manual tasks. Clearing date cells (via Delete/Backspace key or calendar Clear button) automatically switches task to manual scheduling mode. When switching to manual mode, Duration and Estimated Hours are also cleared. Clearing Duration via Delete/Backspace also clears dates and switches to manual mode.
**Custom Fields**: Organization-scoped custom field definitions with `entityType` supporting 'project', 'task', and 'resource' levels. Separate value tables (`project_custom_field_values`, `task_custom_field_values`, `resource_custom_field_values`) store field values per entity. Settings UI provides tabbed management (Project/Task/Resource tabs) with entity type selector in the add/edit dialog. Project-level consumers filter definitions by `entityType='project'`.
**Schedule PNG Export**: Enables exporting the visible schedule as a PNG image.
**Timesheet History Tab**: Displays past timesheet entries grouped by week, with selectable date ranges and status filters.
**Critical Path Method (CPM)**: Implemented for calculating ES/EF/LS/LF/TF for tasks using forward and backward passes, supporting various dependency types and lag.
**Dependency Type Selection**: Full-stack support for FS/SS/FF/SF dependency types with lag days, including API routes, UI selectors, and server-side date adjustments.
**Organization Scheduling Defaults**: Allows defining and enforcing default dependency types and lag days at the organization level.
**Professional Profile & Analytics**: Features a user profile with credentials, an analytics dashboard displaying engagement statistics, a weekly activity chart, feature usage, a tiered professional ranking system, and an achievement badge gallery.
**Shareable Branded Badges**: Allows users to share their profile and earned badges externally via branded images and a public profile page.
**Project Templates Module**: Allows users to create reusable project templates from file uploads (MPP/XML/CSV) or existing projects, with CRUD operations and project creation from templates.
**AI Create Features**: Incorporates AI for smart project matching and context-aware generation of tasks, risks, issues, and milestones, with all actions logged.
**Timesheet Control & Audit Engine**: Organization-level timesheet policies (mandatory notes, overtime threshold, min/max weekly hours, grace period). Full audit logging of all timesheet mutations. Proxy entry support for admins. Compliance reporting dashboard.
**Manager Review Workflow Enhancements**: Approval delegation, rejection templates, comments/history threads on timesheet entries, and a team review dashboard for managers with SLA tracking.
**Reminder & Escalation System**: Automated multi-channel reminder engine for timesheets, including submission reminders, manager approval reminders, auto-escalation, and weekly manager digest emails, configurable at the organization level.
**Partner Program**: A public landing page targeting PMO consulting firms, independent consultants, and trainers/educators, featuring an application form and partner type cards. Applications are stored in a dedicated database table.
**Cross-Project References**: Bidirectional linking between tasks and projects across different projects within the same organization, supporting various relationship types. Integrated into the task edit dialog and project summary.
**Friday Agent (Jarvis)**: AI-powered project assistant accessible via floating orb button. Backend service queries organization's project data (projects, tasks, issues, resources, dependencies) and feeds context to OpenAI for intelligent responses. Uses OpenAI function/tool calling to actually execute write actions (create tasks, risks, issues, add notes, flag projects) when the user confirms — actions are executed server-side via database operations, not just described in text. Features include voice input/output via Web Speech API, "Hey Friday" wake word detection, concise mode toggle, clickable entity links in responses, and sessionStorage-persisted chat history. **Page-scoped context**: Agent detects the current page (project/portfolio/resource) via URL path and sends `pageContext` to the server, which injects focused entity data into the system prompt so the agent prioritizes the entity the user is currently viewing. Context-aware suggested prompts change based on the current page. **File attachments**: Users can attach up to 5 files (500KB each) to agent messages — text-based files (CSV, JSON, TXT, etc.) are base64-decoded and included in the AI prompt; a paperclip button and file preview chips are shown in the input area. **CSV task import**: When a CSV file is attached, the agent parses it, maps flexible column names (e.g. "Task Name", "Owner", "Due Date") to task fields, presents a summary, and uses `bulk_create_tasks` tool to create up to 200 tasks at once in the target project. **Task updates**: `update_task` tool allows changing status, priority, assignee, dates, progress, description, milestone/critical flags on any task. Includes status/progress auto-sync (completing sets 100%, progress > 0 sets In Progress). **Resource assignment**: `assign_resources_to_task` tool assigns organization resources to tasks via the `task_resource_assignments` join table, supporting allocation percentages and validating resource IDs belong to the org. **Resource creation**: `create_resource` and `bulk_create_resources` tools allow the agent to add new resources (people/team members) to the organization with fields like name, email, title, department, skills, experience level, hourly rate, availability, etc. These are org-scoped tools that don't require a project ID. Files: `server/services/jarvisService.ts`, `server/routes/jarvisRoutes.ts`, `client/src/hooks/use-jarvis.ts`, `client/src/hooks/use-speech.ts`, `client/src/hooks/use-wake-word.ts`, `client/src/components/jarvis/JarvisOrb.tsx`, `client/src/components/jarvis/JarvisPanel.tsx`.
**Investor Room**: Password-gated pitch deck page at `/investor-room` with animated slides, PMO Radar demo, pricing tiers, roadmap, and competitive matrix. Includes PDF download via jsPDF + html-to-image, and email PDF dialog. Backend routes handle password verification, access checks, and email sending with authorization enforcement. Navigation links in Home, SignInPage, LandingFooter, and SuperAdmin. Files: `client/src/pages/InvestorRoom.tsx`, `server/routes/investorRoutes.ts`.
**Daily Logs (Field Management)**: A Procore-inspired daily log module for tracking daily site activity. Schema tables: `daily_logs`, `daily_log_labor`, `daily_log_equipment`. Features weather conditions, labor tracking (by company/trade with headcount and hours), equipment tracking (with status and hours), visitor logs, and general notes. Accessible as a project tab ("Daily Logs") and as a standalone sidebar page (`/daily-logs`). CRUD API at `/api/projects/:projectId/daily-logs` with summary endpoint. Date range filtering and summary statistics (total days, labor headcount/hours, equipment counts/hours). Module gated via `daily-logs` key in ModuleGuard/sidebar structure.
**RFIs & Submittals (Construction Management)**: Procore-comparable modules for managing Requests for Information (RFIs) and Submittals. Schema tables: `rfis`, `rfi_responses`, `submittals`, `submittal_revisions`. RFIs support threaded responses with official/unofficial designation, auto-status transitions (Open→Answered on official response), priority levels, cost/schedule impact tracking, and due dates. Submittals support revision-based workflow with review actions (Approve/Reject/Revise & Resubmit), reviewer assignment, spec sections, lead time, and type classification. Both modules include CRUD APIs with strict Zod validation, query param validation, soft-delete with defensive WHERE clauses, and module gating via `rfis`/`submittals` keys. Accessible as project tabs and standalone sidebar pages (`/rfis`, `/submittals`). Files: `server/routes/rfiRoutes.ts`, `server/routes/submittalRoutes.ts`, `client/src/components/project/RFIsTab.tsx`, `client/src/components/project/SubmittalsTab.tsx`, `client/src/pages/RFIs.tsx`, `client/src/pages/Submittals.tsx`.
**Drawings (Construction Management)**: Drawing management module for uploading, organizing, versioning, and marking up construction drawings. Schema tables: `drawing_sets`, `drawings`, `drawing_revisions`, `drawing_markups`. Drawing Sets allow grouping related drawings (e.g., "Foundation Package") with CRUD at `/api/projects/:projectId/drawing-sets`. Drawings support discipline categorization (Architectural, Structural, MEP, etc.), optional drawing set assignment, revision tracking with file upload via object storage, status tracking (Current/Superseded/Void), and inline markup annotations (text, arrows, rectangles, freehand) saved as JSONB. Built-in drawing viewer with zoom/pan, markup overlay, and side-by-side revision comparison mode for comparing any two revisions. Search covers both title and drawing number. CRUD APIs at `/api/projects/:projectId/drawings` with revision and markup sub-endpoints. All route types use `SQL[]` (no `any` casts). Module gated via `drawings` key. Accessible as project tab and standalone sidebar page (`/drawings`). Files: `server/routes/drawingRoutes.ts`, `client/src/components/project/DrawingsTab.tsx`, `client/src/pages/Drawings.tsx`, `client/src/hooks/use-drawings.ts`.
**Punch List (Construction Management)**: Punch list module for tracking deficiencies and outstanding items during project close-out. Schema tables: `punch_items`, `punch_item_photos`. Punch items support title, description, location, category (Architectural, Electrical, Mechanical, etc.), priority (Low/Medium/High/Critical), status workflow (Open → In Progress → Ready for Review → Closed), assignee, due date, and photo attachments (before/after/general types). Photos uploaded via presigned URL flow to object storage. Summary endpoint provides status/priority breakdowns and completion percentage with progress bar. CRUD API at `/api/projects/:projectId/punch-items` with bulk creation endpoint. Photo sub-endpoints for adding/removing photos per item. Filtering by status, priority, category, assignee, and location. Detail view shows full item info with inline photo gallery and quick status transition buttons. Module gated via `punch-list` key. Accessible as project tab and standalone sidebar page (`/punch-list`). Files: `server/routes/punchListRoutes.ts`, `client/src/components/project/PunchListTab.tsx`, `client/src/pages/PunchList.tsx`, `client/src/hooks/use-punch-list.ts`.
**Quality & Safety (Construction Management)**: Quality and safety management module with inspections, incidents, observations, and a safety dashboard. Schema tables: `inspection_templates`, `inspection_template_items`, `inspections`, `inspection_results`, `incidents`, `incident_actions`, `observations`, `observation_actions`. Inspection templates define reusable checklists with sections and pass/fail items. Inspections support scheduling, inspector assignment, template-based execution with per-item results (Pass/Fail/N/A), deficiency tracking, corrective actions, and automatic overall result calculation. Incidents track safety events with severity levels (Minor to Fatal), status workflow (Reported → Under Investigation → Corrective Action → Closed), injured parties, witnesses, root cause analysis, and linked corrective actions. Observations support positive/negative categorization across Safety/Quality/Environmental categories with severity, corrective actions, and follow-up action tracking. Safety Dashboard provides aggregate statistics including inspection completion rates, incident severity breakdown, observation category distribution, and open corrective action counts. All entities use safe number generation with `FOR UPDATE` locking and unique indexes. CRUD APIs under `/api/projects/:projectId/` with strict Zod validation, project-scoped access control, and parent entity validation for nested action endpoints. Module gated via `quality-safety` key. Accessible as project tab (with Dashboard/Inspections/Incidents/Observations/Templates subtabs) and standalone sidebar page (`/quality-safety`). Files: `server/routes/qualitySafetyRoutes.ts`, `client/src/components/project/QualitySafetyTab.tsx`, `client/src/pages/QualitySafety.tsx`, `client/src/hooks/use-quality-safety.ts`.
**Bidding & Preconstruction (Construction Management)**: Bidding and preconstruction module for managing vendor directories, prequalification, bid packages, bid invitations, bid submissions, and bid leveling (side-by-side comparison). Schema tables: `vendors`, `vendor_prequalifications`, `bid_packages`, `bid_invitations`, `bids`, `bid_line_items`. Vendors are org-level entities with contact info, trade specialty, license/insurance/bonding details, and star ratings. Vendor prequalifications track safety/financial/quality ratings, experience, EMR rates, certifications (OSHA 300, insurance cert, bonding letter), and qualification status workflow (Pending → Qualified/Conditionally Qualified/Disqualified). Bid packages are project-level with sequential BID-#### numbering, trade categories, scope of work, estimated budget, pre-bid and due dates, and status workflow (Draft → Open → Closed → Under Review → Awarded → Cancelled). Bid invitations link vendors to bid packages with response tracking. Bids capture total/alternate amounts, bond inclusion, exclusions, clarifications, validity dates, evaluation scores (0-100), and recommended bid flagging. Bid line items provide detailed cost breakdown per bid. Bid leveling view provides side-by-side comparison of all bids with summary statistics (lowest/highest/average/recommended), criteria comparison table, and line item comparison matrix. CRUD APIs at `/api/organizations/:orgId/vendors` (org-level) and `/api/projects/:projectId/bid-packages` (project-level) with nested endpoints for invitations, bids, and leveling. Module gated via `bidding` and `vendors` keys. Accessible as project tab ("Bidding") and standalone sidebar pages (`/bidding`, `/vendors`). Files: `server/routes/biddingRoutes.ts`, `client/src/components/project/BiddingTab.tsx`, `client/src/pages/Bidding.tsx`, `client/src/pages/Vendors.tsx`, `client/src/hooks/use-bidding.ts`.
**Change Orders & Construction Invoices**: PCO→COR→CO tier workflow for change order management with line items, approval with project_financials audit trail, and contract summary computation. Construction invoices with payment applications, aging reports (5-bucket), and record payment flow. Module keys: `change-orders`, `construction-invoices`.
**Meetings & Collaboration**: Meeting management with agendas, minutes recording, and action item tracking. Meetings have sequential numbering (MTG-###), meeting types (General, Standup, Review, Planning, etc.), and attendees. Action items are tracked across the project with assignee, due date, priority, and status. Consolidated action items view. Schema tables: `meetings`, `meeting_agenda_items`, `meeting_action_items`. Module key: `meetings`.
**Correspondence**: Formal project correspondence tracking with types (Letter, Email, Transmittal, Notice), sequential numbering per type (LT-###, EM-###, TR-###, NT-###), from/to parties, status workflow (Draft→Sent→Received→Responded→Closed), and priority levels. Schema table: `correspondence`. Module key: `correspondence`.
**Database Migration & Deployment**: The build process includes schema synchronization and additional index/manual migrations.
**Schema Integrity**: A script validates all tables, columns, and foreign keys.
**KPI Analytics Dashboard**: A dedicated dashboard tab under Dashboards showing org-scoped user activity metrics across time cohorts (Week 1-4, Month 2-3, Month 4+). Tracks tasks created/completed, projects created, issues raised/resolved, hours logged, feature usage, active users, and platform engagement. Backend endpoint at `/api/dashboard/kpi-metrics` aggregates data from tasks, projects, issues, timesheets, feature usage logs, and change logs. Frontend uses Recharts for bar charts, line charts, and area charts with a detailed breakdown table.
**Super Admin KPI Analytics**: A platform-wide KPI Analytics tab in the Super Admin Console (`/super-admin`) for monitoring all subscriber activity and engagement. Backend endpoint at `/api/admin/kpi-metrics` aggregates data across all organizations/users (no org filter). Includes new signups metric, user behavior stats (top features by usage with read/write breakdown and response times), error hotspots (features with high error rates and affected user counts), and a friction trend chart (weekly error rate vs active users over 90 days). All behavior data sourced from `api_request_logs`. Only accessible to Super Admin and Marketing roles.

## External Dependencies

-   **Database**: PostgreSQL
-   **ORM**: Drizzle ORM
-   **Authentication**: Replit Auth (OpenID Connect), Passport.js, express-session
-   **UI Libraries**: shadcn/ui, Radix UI, Lucide React
-   **Integrations**:
    -   Microsoft Project (MPXJ)
    -   Microsoft Planner (Microsoft Graph API)
    -   Microsoft Planner Premium / Dataverse (msdyn_projecttaskdependencies)
    -   Microsoft Dynamics 365 Sales Hub (OAuth 2.0 / MSAL)
    -   Analytics API